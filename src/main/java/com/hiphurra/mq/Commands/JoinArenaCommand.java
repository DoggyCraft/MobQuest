package com.hiphurra.mq.Commands;

import org.bukkit.command.CommandSender;

@CommandInfo(
        name = "join",
        pattern = "",
        usage = "/ma join <Arena>",
        desc = "",
        permission = ""
)
public class JoinArenaCommand implements Command {

    @Override
    public boolean execute(CommandSender sender, String... args) {

        
        return true;
    }
}

